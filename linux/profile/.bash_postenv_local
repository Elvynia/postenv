export POSTENV_PATH="${POSTENV_PATH:-$HOME/postenv}"

alias postenv="bash $POSTENV_PATH/linux/bootstrap/local.sh"
alias postapply="bash $POSTENV_PATH/linux/apply.sh"

alias postbash="bash $POSTENV_PATH/linux/profile/install.sh \
  --home \$HOME --user \$USER --dev 1 && \
  sudo bash $POSTENV_PATH/linux/profile/install.sh \
    --home /root --user root --dev 1"

alias postgit="bash $POSTENV_PATH/linux/git/install.sh"
